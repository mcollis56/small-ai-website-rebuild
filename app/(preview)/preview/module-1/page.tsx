'use client';

export default function Module1Preview() {
  return (
    <main
      style={{
        minHeight: '100vh',
        background: '#0F172A',
        color: 'white',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: '32px',
        fontWeight: 600,
      }}
    >
      Tutorial Preview — Module 1
    </main>
  );
}
