// Location: app/(preview)/layout.tsx
// This is the isolated layout for all tutorials.

export default function PreviewLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
